# The shell script for setting up with command line prompt 

1. Choose set up method (docker or local) and set up accordingly 
    - If docker, check docker installation, then goest to 2
    - If local, goes to two directly
2 Ask for daabase opption and set up accodingly
3. (only if local) ask for deploying on now.sh?
